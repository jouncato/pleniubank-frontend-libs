# Tasks: extend-breb-key-management-data-access (frontend-libs)

- [x] 1.1 `BrebKeyType` (`wallet.models.ts`) ampliado con `'CUSTOM'`. Añadido además `BrebKeyProposalType = Exclude<BrebKeyType, 'CUSTOM'>` para tipar `AliasProposalItemDto.key_type` (core nunca propone CUSTOM — es de ingreso manual, confirmado en `AliasProposalItem.key_type` del schema real: `"CEDULA | CELULAR | EMAIL"`, sin CUSTOM).
- [x] 1.2 `is_primary: boolean` añadido a `BrebKeySelfServiceDto` (mismo nombre/posición que el DTO real de core) y reflejado en los flush de los tests existentes que lo ejercitan.
- [x] 1.3 `setPrimary(id)` implementado contra `PUT {base}/{id}/primary` — **ruta y verbo verificados leyendo el router real** (`breb_keys_self_service_router.py:449`, `@router.put("/{breb_key_id}/primary", ...)`), no asumidos. Responde `SetBrebKeyPrimaryResponseDto { primary_key: BrebKeySelfServiceDto }`, también verificado contra `SetBrebKeyPrimaryResponse` en `breb_key_self_service_schemas.py`. De paso se agregó `getProposals()` (`POST {base}/proposals`) — requerido por `extend-breb-key-management-ui` (task 1.1 de ese repo) y no listado explícitamente aquí en el proposal original, pero es la misma capability (`virtual-account-data-access`) y sin él la UI queda bloqueada por un tercer eslabón faltante. Tipos: `AliasProposalItemDto`/`AliasProposalsResponseDto`, verificados contra `AliasProposalItem`/`AliasProposalsResponse`.
- [x] 1.4 `CUSTOM_KEY_FORMAT_NOT_ALLOWED` agregado al catálogo central (`resolve-user-facing-api-error.ts`, único mapper permitido — no se tocó el `error-message.mapper.ts` deprecado). Mensaje curado en español sin filtrar el detalle crudo del backend. El 409 anti-enumeración (`BREB_KEY_NOT_ELIGIBLE`) no requirió cambios: el componente consumidor ya usa su propio mensaje genérico local para cualquier error de registro, sin depender de este catálogo para ese código.
- [x] 1.5 Tests nuevos en `core-breb-keys-self-service-api.service.spec.ts`: registro CUSTOM (éxito + 422 `CUSTOM_KEY_FORMAT_NOT_ALLOWED`), `getProposals()` (respuesta con propuestas enmascaradas), `setPrimary()` (éxito + 404 `BREB_KEY_NOT_FOUND`). Test nuevo en `b2c-services-interceptors.integration.spec.ts` confirmando que `getProposals()` también lleva `X-Tenant-Country`/`X-Correlation-ID` (mismo `HttpClient` inyectado que `.list()`, sin bypass). Test nuevo en `resolve-user-facing-api-error.spec.ts` para el código nuevo. **Ejecutados de verdad**: `ng test core-data-access` → 20 archivos, 110 tests, verde; `ng test shared-http` → 9 archivos, 98 tests, verde.
- [x] 1.6 Validación campo-por-campo realizada leyendo directamente `breb_key_self_service_schemas.py` y `breb_keys_self_service_router.py` en `pleniubank-core` (no asumida). **Cero divergencias encontradas** entre los tipos/rutas nuevos y el contrato real:
  - `BrebKeySelfServiceDto`: `id, key_type, is_active, verified, is_primary, created_at, account_id` — coincide exactamente.
  - `AliasProposalItemDto`: `key_type (CEDULA|CELULAR|EMAIL), masked_value, availability (AVAILABLE|TAKEN|UNKNOWN)` — coincide exactamente.
  - `SetBrebKeyPrimaryResponseDto`: `{ primary_key }` — coincide exactamente.
  - Endpoint de principal: `PUT /{id}/primary` (no POST) — confirmado en el decorador real del router, no asumido del proposal (que no especificaba el verbo).
  - Prefijo base: sin cambios — `getProposals()`/`setPrimary()` reutilizan `this.base` (`{corePublicV1Base}/customers/me/breb-keys`), ya validado por los métodos existentes `list()`/`register()`/`remove()`.
- [x] 1.7 `openspec validate extend-breb-key-management-data-access --strict`: en verde.
