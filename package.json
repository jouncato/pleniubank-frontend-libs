{
  "name": "pleniubank-frontend-libs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "ng": "ng",
    "build:all": "ng build design-tokens && ng build ui && ng build shared-http && ng build shared-auth && ng build identity-domain && ng build identity-data-access && ng build identity-feature-auth && ng build identity-feature-enterprise && ng build core-domain && ng build core-data-access && ng build paymenthub-domain && ng build paymenthub-data-access",
    "build:identity-feature-auth": "ng build identity-domain && ng build shared-http && ng build shared-auth && ng build identity-data-access && ng build identity-feature-auth",
    "build:all:prod": "npm run build:all -- --configuration production",
    "test:all": "ng test shared-http --watch=false && ng test shared-auth --watch=false && ng test identity-domain --watch=false && ng test identity-data-access --watch=false && ng test identity-feature-auth --watch=false && ng test identity-feature-enterprise --watch=false && ng test core-domain --watch=false && ng test core-data-access --watch=false && ng test paymenthub-domain --watch=false && ng test paymenthub-data-access --watch=false",
    "verify": "npm run build:all && npm run test:all"
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "packageManager": "npm@11.10.1",
  "dependencies": {
    "@angular/common": "^21.1.0",
    "@angular/compiler": "^21.1.0",
    "@angular/core": "^21.1.0",
    "@angular/forms": "^21.1.0",
    "@angular/platform-browser": "^21.1.0",
    "@angular/router": "^21.1.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/build": "^21.2.4",
    "@angular/cli": "^21.1.4",
    "@angular/compiler-cli": "^21.1.0",
    "jsdom": "^28.0.0",
    "ng-packagr": "^21.2.0",
    "typescript": "~5.9.2",
    "vitest": "^4.0.8"
  }
}
