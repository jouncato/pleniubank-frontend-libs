/**
 * @deprecated Los tipos de reglas se movieron a `@pleniu/rules-types` (HU-RE-046).
 * Este archivo ahora solo re-exporta para mantener backward-compatibility con
 * los consumidores que importan desde `core-data-access`. Los nuevos consumidores
 * deben importar directamente desde `@pleniu/rules-types`.
 */
export * from '@pleniu/rules-types';
